<Window x:Class="AwesomeCommand.Views.ConfirmPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Title="确认密码" Height="160" Width="300"
        Loaded="Dialog_Load">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label>请输入密码：</Label>
        <PasswordBox
            Grid.Row="1" Margin="4"
            VerticalContentAlignment="Center"
            x:Name="Password"
            />
        <Label
            Grid.Row="2"
            x:Name="TipsLabel"
            Foreground="Red"
            HorizontalContentAlignment="Center"
            />
        <Button
            Grid.Row="3" Margin="4,4,4,0"
            Content="确定" Height="24"
            Click="ConfirmButton_Click"
            />
    </Grid>
</Window>
